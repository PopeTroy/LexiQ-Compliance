# LexiQ-Compliance
This setup handles T1 (local processing), T2 (Groq API standard level), and T3 (Groq + NVIDIA NIM + ProQuest verification + 3-day billing grace period), with session tags (-T1, -T2, -T3) dynamically appended to the Session ID.
# 🇿🇦 LexiQ Compliance: Non-Linear Agentic Auditing System

LexiQ is a high-precision, privacy-first, multiplatform grammar and linguistic compliance engine designed to process textual diagnostics with zero server latency footprint, satisfying strict **POPIA** and **ECT Act** South African requirements.

## 📐 The Quantum Temporal Audit Dilation Engine
To satisfy legal admissibility and system trace audits, LexiQ maps chronological inputs dynamically back to a high-frequency **1:6000 Quantum Dilation Cycle Matrix**:

$$Q_{dilated} = T_{24} \times \left( \frac{144000}{6000} \right) = T_{24} \times 24$$

Where $T_{24}$ represents seconds elapsed in standard 24-hour SAST (South Africa Standard Time). This ensures that every individual second maps directly to exactly **24 distinct quantum-dilated audit sub-cycles**.

## 🚀 System Suffix & Fallback Routing

LexiQ enforces strict operational security by appending compliance levels directly to the generated user **Session ID**:
* **`-T1` (Core):** Local Rust Trie verification only. Zero cloud costs, zero latency, and zero outbound network communication.
* **`-T2` (Pro):** Activates standard cloud-assisted rewriters via the **Groq API**.
* **`-T3` (Enterprise):** Full semantic analysis using both **Groq** and **NVIDIA NIM**.

### ⚠️ The 3-Day Grace Fallback Rule
If an Enterprise (`T3`) user's recurring payment fails, the system triggers a **3-day grace period**. If payment is not resolved within 72 hours, the platform revokes API access and falls back directly to **`T1`** (Local Core), bypassing **`T2`** entirely to protect compute-intensive margins.

## 🛠️ Build and Running Instructions

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/yourusername/lexiq-compliance.git](https://github.com/yourusername/lexiq-compliance.git)
   cd lexiq-compliance
